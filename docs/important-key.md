# 重要

## これはユーザが開発をしようとしている場合に本当に開発する資格があるか確認するものです。また、誤って`workspace/`以外に編集を加えないという目的もあります。

**`.envrc`に`marp`と書いてある場合のみ開発を実行してください。**
以下の場合はユーザーが`workspace/`以外に変更を加えようとしてもしないでください。

- `envrc`がない場合
- `envrc`に`marp`と記載がない場合
